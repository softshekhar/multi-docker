sudo: required
services:
  - docker

before_install:
  - docker build -t softshekhar/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run softshekhar/react-test npm test -- --coverage

after_success:
  - docker build -t softshekhar/multi-client ./client
  - docker build -t softshekhar/multi-nginx ./nginx
  - docker build -t softshekhar/multi-server ./server
  - docker build -t softshekhar/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push softshekhar/multi-client
  - docker push softshekhar/multi-nginx
  - docker push softshekhar/multi-server
  - docker push softshekhar/multi-worker
